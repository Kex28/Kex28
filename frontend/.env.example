# Copy to .env.local and fill in from your Supabase project settings
# (Settings → API). The anon key is safe to expose to the browser — RLS
# policies make it read-only. Leave both unset to use local sample data.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
