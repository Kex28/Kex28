import ComingSoon from '../components/ComingSoon'

export default function CounterMeta() {
  return (
    <ComingSoon
      title="Counter Meta"
      subtitle="Tech decks that beat the current top decks."
      phase="Phase 3"
      details="Flags archetypes with a strong record specifically against the current top 3–5 decks but a mediocre overall record. Requires match-level data (gated API key) so each pairing can be identified from both sides."
    />
  )
}
