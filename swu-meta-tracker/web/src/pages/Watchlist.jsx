import ComingSoon from '../components/ComingSoon'

export default function Watchlist() {
  return (
    <ComingSoon
      title="Watchlist"
      subtitle="Decks and archetypes you're personally tracking."
      phase="Phase 4"
      details="Save specific archetypes to your own watchlist for updates. The watchlist table and per-user security policies are already in the database schema, so this is UI work once the trend pages exist."
    />
  )
}
